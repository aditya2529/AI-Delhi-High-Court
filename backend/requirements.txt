# ─────────────────────────────────────────────────────────────────────────────
# Delhi HC Case Tracker — backend dependencies (FastAPI + httpx)
# Python 3.11+. Pin to minor versions; bump intentionally in PRs.
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0

# ── Outbound HTTP (async; preserves cookies + CSRF tokens) ────────────────
httpx==0.27.2

# ── HTML parsing (defensive — multiple fallback selectors) ────────────────
beautifulsoup4==4.12.3
lxml==5.3.0

# ── Database (SQLAlchemy 2.x async + Alembic) ─────────────────────────────
sqlalchemy[asyncio]==2.0.36
alembic==1.13.3
aiosqlite==0.20.0
# Uncomment when migrating to PostgreSQL in v2:
# asyncpg==0.30.0

# ── Caching / session backend (optional) ──────────────────────────────────
redis[hiredis]==5.2.0

# ── robots.txt parsing (we respect it as kill-switch) ─────────────────────
# urllib's built-in robotparser is sufficient for v1; no external dep needed.

# ── Logging / observability ───────────────────────────────────────────────
structlog==24.4.0

# ── Dev / test ────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx-mock==0.0.0  # alias placeholder; consider respx for httpx mocking
respx==0.21.1
ruff==0.7.4
mypy==1.13.0
pip-audit==2.7.3   # CI dependency-scan
